# Don't use LFS for publication preview images
assets/img/publication_preview/*.png -filter -diff -merge -text
assets/img/publication_preview/*.gif -filter -diff -merge -text

# Use LFS for other images
*.png filter=lfs diff=lfs merge=lfs -text
MtC_2025_1.JPG filter=lfs diff=lfs merge=lfs -text
* !text !filter !merge !diff
